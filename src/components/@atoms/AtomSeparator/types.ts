import { ViewStyle } from "react-native";
import { CSS } from "~/types";

export interface AtomSeparatorTypes {
  borderBottomWidth?: string;
  borderBottomColor?: string;
  customStyles?: ViewStyle;
  margin?: string;
  css?: CSS;
}
